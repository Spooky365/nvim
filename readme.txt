 Location homeofuse/.config/nvim
 if it it gets errors because of telescore fzf 
 need to make it again;
 cd /Users/ll/.local/share/site/pack/packer/start/telescope-fzf-native.nvim && make clean && make
